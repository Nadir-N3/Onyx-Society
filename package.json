{
  "name": "onyx-society",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network mainnet",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "allowlist": "node merkle/generateAllowlist.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.9",
    "merkletreejs": "^0.3.11",
    "keccak256": "^1.0.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}